apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-setter-config
data:
  tag: "latest"
  replicas: "1"
  maxUnavailable: "1"
  resources.requests.memory: "256Mi"
  resources.requests.cpu: "1000m"
  resources.limits.memory: "256Mi"
  resources.limits.cpu: "1000m"
